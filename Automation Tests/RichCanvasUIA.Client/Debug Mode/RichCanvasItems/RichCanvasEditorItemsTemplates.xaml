<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:items="clr-namespace:RichCanvasUIA.Client.Debug_Mode.RichCanvasItems"
                    xmlns:rc="clr-namespace:RichCanvas;assembly=RichCanvas">
    <DataTemplate DataType="{x:Type items:RichCanvasEditorItem}">
        <Rectangle Fill="Red">
            <Rectangle.Style>
                <Style>
                    <Setter Property="rc:RichCanvasContainer.ApplyTransform">
                        <Setter.Value>
                            <TransformGroup>
                                <RotateTransform Angle="{Binding Angle}" />
                                <ScaleTransform />
                                <TranslateTransform />
                            </TransformGroup>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Rectangle.Style>
        </Rectangle>
    </DataTemplate>
    <DataTemplate DataType="{x:Type items:LineEditorItem}">
        <Line  Stroke="Red"
               StrokeThickness="3"
               X2="{Binding Width}"
               Y2="{Binding Height}">
        </Line>
    </DataTemplate>
</ResourceDictionary>