<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:RichCanvas">
    <Style TargetType="{x:Type local:RichItemContainer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:RichItemContainer}">
                    <Border BorderBrush="Black"
                            BorderThickness="1"
                            Background="Transparent"
                            IsHitTestVisible="True">
                        <ContentPresenter x:Name="PART_ContentPresenter"></ContentPresenter>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>